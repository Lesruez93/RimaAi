# RimaAI web configuration.
# Base URL of the FastAPI backend the dashboard reads from.
# Defaults to http://localhost:8000 when unset. If the backend is unreachable,
# the dashboard renders bundled demo data instead.
NEXT_PUBLIC_API_BASE=http://localhost:8000
