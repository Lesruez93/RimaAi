# Screenshots

Add app + simulator screenshots here for the README and proposal appendix:

- `home.png` — dashboard with seasonal forecast card
- `scan_result.png` — crop scan result + advice
- `triage.png` — livestock triage flagging January disease
- `alerts.png` — subscription toggles + consent
- `outbreak_map.png` — Zimbabwe heat map with risk markers
- `guard.png` — Guard camera view with detection boxes
- `ussd.png` — USSD simulator mid-flow

Capture with `flutter run` (device/emulator) and the simulator page in a browser.
