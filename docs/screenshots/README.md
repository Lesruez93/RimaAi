# Screenshots

Add app + simulator screenshots here for the README and proposal appendix:

- `dashboard.png` — officer dashboard with district risk, alerts and Guard events ✅ (also embedded on the landing page)
- `simulators.png` — the WhatsApp and USSD simulators mid-conversation ✅ (also embedded on the landing page)
- `app_onboarding.png` — language selection (English / chiShona / isiNdebele) ✅ (also embedded on the landing page)
- `app_home.png` — app home with seasonal planting-window forecast card ✅ (also embedded on the landing page)
- `app_scan.png` — scan-a-crop capture screen (camera / gallery) ✅ (also embedded on the landing page)
- `app_livestock.png` — livestock health check (photo check + symptom chat) ✅ (also embedded on the landing page)
- `app_outbreak_report.png` — report-an-outbreak form (type, district, description) ✅ (also embedded on the landing page)
- `app_alerts.png` — subscribe-to-alerts screen with consent ✅ (also embedded on the landing page)
- `app_outbreak_map.png` — Zimbabwe heat map with district risk markers ✅ (also embedded on the landing page)
- `guard.png` — Guard camera view with detection boxes

Capture with `flutter run` (device/emulator) and the simulator page in a browser.
`dashboard.png` / `simulators.png` were captured from the live web app + backend
via a headless browser at 2x device scale.
